#include<stdio.h>
#include<string.h>

int main(int argc ,char *argv[])
{
        FILE *file;
        file = fopen(argv[1],"r");
        
        char line[256];
	int lines = 0;
        while(fgets(line, sizeof line, file)){
	    lines++;  
	      }  
	
	int idx = 0;
	fclose(file);
	
	FILE *file2;
        file2 = fopen(argv[1],"r");
        struct content{
        	char element[256];
        	};
        
        struct content app[lines];
        int idx2 = 0;
        while(fgets(line, sizeof line, file2)){
	    strcpy(app[idx2].element, line);
	    idx2++;
	      }    
	int i = 0;
        for(int i = lines - 1; i >= 0; i--)
            printf("%s", app[i].element);

    fclose(file2);
    return 0;
}