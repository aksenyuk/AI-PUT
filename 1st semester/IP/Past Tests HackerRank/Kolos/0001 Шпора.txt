# Transpose matrix
transposed = [[x[i] for x in matrix] for i in range(rows)]

# Generating matrix with int values
matrix = [[int(x) for x in input().split()] for i in range(rows)]

# Copying matrix and making it independent
new = [x[:] for x in matrix]

# Removing Duplicates from list
potential = list(dict.fromkeys((potential)))

#Sort dictionary where students - dictionary
[print(r, students[r]) for r in sorted(students)]
    